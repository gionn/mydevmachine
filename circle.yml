machine:
  ruby:
    version: 2.1.0
  services:
    - docker

dependencies:
  override:
    - docker info
    - gem install berkshelf
    - gem install test-kitchen
    - gem install kitchen-docker

test:
  override:
    - kitchen test
